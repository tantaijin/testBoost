#include "stdafx.h"
/*
#include "multi_array_example.h"
#include "boost\multi_array.hpp"

void test_multi_array()
{
	boost::multi_array<int, 3> ma(boost::extents[2][3][4]);

	int v = 0;
	for (int i = 0; i < 2; i++)
	{
		for (int j = 0; j < 3; j++)
		{
			for (int k = 0; k < 4; k++)
			{
				ma[i][j][k] = v;
			}
		}
	}
	for (int i = 0; i < 2; i++)
	{
		for (int j = 0; j < 3; j++)
		{
			for (int k = 0; k < 4; k++)
			{
				std::cout << ma[i][j][k] << " ";
			}
		}
	}
	std::cout << std::endl;
}
*/